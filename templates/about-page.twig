{# DIT IS EEN TEMPLATE -- HEAD EN BODY STEKEN IN DE INDEX-TWIG #}
{% extends "_index" %}

{% block content %}

{% include "_snippets/_navbar" %}
{# DIT IS HET BEGIN VAN DE BLOCK #}

{% set imageTest = entry.imagetest.one() %}

<section class="container-fluid p-md mt-xl">   
    <h1 class="txt-heading_lg">{{entry.title}}</h1>
    <img src={{ imageTest.url }} alt={{ imageTest.title }}></img>
    <p class="txt-body_md">{{entry.description}}</p>
</section>

{% endblock %}
